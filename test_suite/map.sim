#!/usr/bin/env simple

$$names list("Albert" "Berta" "Caesar")
$$shouting_names map(names to_upper_case)
dump 2 `shouting_names

$name "Albert"
$encoded_name map(name: (chr) -> chr+1)
dump 2 `encoded_name
